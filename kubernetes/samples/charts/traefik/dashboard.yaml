apiVersion: v1
kind: Service
metadata:
  labels:
    app: traefik
    chart: traefik-1.43.0
    heritage: Tiller
    release: traefik-operator
  name: my-traefik-dashboard
  namespace: traefik
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
    nodePort: 30311
  selector:
    app: traefik
    release: traefik-operator
  sessionAffinity: None
  type: NodePort
