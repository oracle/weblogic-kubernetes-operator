1.  The updatecm.sh  will create a configmap named wdt-config-map from the current subdirectory cm using namespace sample-domain1-ns
2.  Change to sample domain resource yaml to fit your environment and then apply it to setup the weblogic operator domain
