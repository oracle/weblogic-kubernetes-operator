apiVersion: v1
kind: PersistentVolume
metadata:
  name: weblogic-domain-logs-pv

spec:
  storageClassName: weblogic-domain-logs-storage-class
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: "%PATH%"
